{
  "name": "mastercv-functions",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "files": [
    "lib"
  ],
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "predeploy": "npm run build",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "cd .. && npx firebase deploy --only functions:linkedinAnalysisProcessor",
    "lint": "eslint . --ext .ts"
  },
  "dependencies": {
    "@google/generative-ai": "^0.12.0",
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "eslint": "^8.57.0",
    "typescript": "^5.4.0"
  }
}
